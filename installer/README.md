Fixbot installer UI

Open index.html in a browser and click Install Fixbot.
If the browser blocks running the .cmd file, open an elevated CMD and run:

installer\install.cmd

The installer copies Fixbot to Program Files, creates an admin shortcut,
and adds a fixbot command to PATH for admin CMD usage.
